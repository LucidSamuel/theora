{
  "name": "@theora/mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing theora's cryptographic primitives as tools for AI agents",
  "type": "module",
  "bin": {
    "theora-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node build/index.js",
    "test": "npm run build && node --test test/*.test.mjs",
    "verify": "npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.6.2"
  },
  "engines": {
    "node": ">=18"
  }
}
